"""
    random_moves_without_replacement(neighbourhood, solution) -> iterable

Return an iterable of moves from `neighbourhood` for `solution` in random
order without replacement.
"""
function random_moves_without_replacement end
